# PDF → JSON Pipeline (Demo)
Drop any PDF → get structured JSON + confidence flags in 30 s.
## Stack
AWS Textract • GPT-4 • Python
## Run
1. pip install -r requirements.txt
2. python demo.py
Output: output.json
